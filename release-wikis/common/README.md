# Common Wiki publication set

Destination: `ValentinNikolaev/php-upgrade-preflight` GitHub Wiki.

This is the broad product and monorepo Wiki. Its manifest includes package pages
because the monorepo is the umbrella repository. Publish only entries from
`pages/*.md`; do not combine this set with a package manifest. Regenerate and check
the tree with the root commands in `release-wikis/README.md`.
